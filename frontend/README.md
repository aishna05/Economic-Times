# Frontend

Next.js app for the Economic Times multimodal news assistant prototype.

## Run

```bash
cd frontend
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE=http://localhost:5000` if the backend runs on a different host.
